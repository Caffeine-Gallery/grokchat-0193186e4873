service : {
  addFiles: (vec text) -> (text);
  addMessage: (text, bool) -> ();
  editFiles: (vec text, text) -> (text);
  getConversationHistory: () -> (vec text);
  getLastResponse: () -> (text);
  resetContext: () -> ();
  reviewCode: (vec text) -> (text);
}
